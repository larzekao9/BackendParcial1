# Configuración de Claude Code — no se publica en el repo del proyecto
.claude/
CLAUDE.md
memory/
.mcp.json

# Documentos de planificación de la sesión (no forman parte de la
# documentación oficial del proyecto)
docs/plan-backend.md
docs/contratos-servicios.md
docs/plan-luis-angel.md

# Backend (Node/NestJS)
Backend/node_modules/
Backend/dist/
Backend/.env
Backend/coverage/
Backend/*.log

# Credenciales (Google Cloud, Android/Flutter cuando exista mobile-app/) —
# nunca se suben, sin importar en qué carpeta del repo aparezcan.
client_secret_*.json
google-services.json
GoogleService-Info.plist
*.keystore
*.jks
key.properties
*.p12
*.pem

# SO
.DS_Store

# Salida generada por graphify (grafo de conocimiento local, no es parte
# del código del proyecto)
graphify-out/

# Repo de OTRO trabajo grupal (Taller SW2, ajedrez), clonado localmente en
# esta carpeta por error/aparte — no es parte del Sistema de Cine, nunca se
# sube a este repo.
ProyectGrupal_Taller_SW2_Frontend/
Backend/tsconfig.build.tsbuildinfo
Backend/*.tsbuildinfo
